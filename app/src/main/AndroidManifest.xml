<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imagine.mohamedtaha.store">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Add_DAILY_MOVEMENTS" />
        <activity android:name=".DialogAddCategory" />
        <activity android:name=".DialogAddStore" />
        <activity android:name=".ActivityForIncludeFragments" />

        <provider
            android:name=".data.TaskContentProvider"
            android:authorities="com.imagine.mohamedtaha.store"
            android:exported="false" />
        <provider
            android:name=".data.StoreContentProvider"
            android:authorities="com.imagine.mohamedtaha.store"
            android:exported="false" />

        <activity
            android:name=".EditCategory"
            android:parentActivityName=".ActivityForIncludeFragments">

            <!-- PArent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityForIncludeFragments" />
        </activity>
        <activity android:name=".EditStore" />
        <activity android:name=".StockingWarehouse" />
        <activity android:name=".EditStokeWarehouse" />
        <activity android:name=".EditDailyMovements"></activity>
    </application>

</manifest>